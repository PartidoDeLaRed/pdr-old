#compose.compose-modal(role="dialog", aria-hidden="true", style="display:none;")

  form.compose-modal-inner(method="post", action="/issues/process", autocomplete="off")

    h2  Crear problematica

    p.description Las problemáticas son el primer paso para la toma de decisiones y el inicio de las acciones. Son el combustible para las ideas e ideologías de una sociedad en red.

    input(type="hidden", name="issue[_id]")

    p.insertion
      span.insertion-point
      select(name="issue[category]")
        - each category, cid in categories
          option(value=cid)= category.name

    p.insertion
      span.insertion-point
      input.compose-title.compose-text(name="issue[title]", type="text", placeholder="Titulo de la problematica")


    p.insertion
      span.insertion-point
      textarea.compose-content.compose-text(name="issue[essay]", placeholder="Contenido de la problematica")

    input(type="submit", class="btn", value="Crear problematica") 

    a.close-compose(href="", data-dismiss="modal") ×

