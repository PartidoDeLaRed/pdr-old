.comments.span9
  article.box.clearfix
    h3.block-title
      i
      | Comentarios sobre la #{elementType.toLowerCase()} "
      em= element.title
      | "
    include comment_form
    .comment-list
      - comments.forEach(function(comment) {
        include comment_single
      - });